import * as React from 'react';
import { {{name}} } from './';
import { assert, describe, expect, it } from 'vitest'


describe('Test for the {{ name }} component', () => {
  beforeEach(() => {
  
  })

  afterEach(() => {
 
  })
  it('First test', () => {
  })


})

import * as React from "react";
import { LoadingSphere } from "./";
import { afterEach, assert, beforeEach, describe, expect, it } from "vitest";

describe("Test for the LoadingSphere component", () => {
  beforeEach(() => {});

  afterEach(() => {});
  it("First test", () => {});
});

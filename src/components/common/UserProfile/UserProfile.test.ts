import * as React from "react";
import { UserProfile } from "./";
import { afterEach, assert, beforeEach, describe, expect, it } from "vitest";

describe("Test for the UserProfile component", () => {
  beforeEach(() => {});

  afterEach(() => {});
  it("First test", () => {});
});
